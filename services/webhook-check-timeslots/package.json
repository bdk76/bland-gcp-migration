{
  "name": "webhook-check-timeslots",
  "version": "1.0.0",
  "description": "Webhook for checking and ranking available appointment time slots",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@google-cloud/firestore": "^7.1.0",
    "moment-timezone": "^0.5.43"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
